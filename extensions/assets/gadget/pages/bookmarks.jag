<%
var log = new Log(),
        caramel = require('caramel'),
        recipe-app = require('/modules/recipe-app.js');
var server = require('store').server;

caramel.render({
    op: 'test',
    title: 'Publisher Extensions | Gadgets',
    shortName: 'gadget',
    cuser: server.current(session)
});
%>